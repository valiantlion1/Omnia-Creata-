# Archive

This directory stores historical material that still belongs in the repository.

- `docs/legacy/` keeps old review packs, blueprint bundles, and system report material
- `products/` keeps product-specific legacy snapshots that are still useful for reference
- `_cold-storage/` is for heavy local-only exports and is gitignored
