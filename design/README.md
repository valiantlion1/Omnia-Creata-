# Design

Use this area for:

- `brand/` shared logos and brand references
- `figma/` imported or exported Figma-derived material
- `exports/` reusable creative exports that belong at the ecosystem level

Runtime asset files that belong to a single product should stay inside that product.
