# OmniaPixels 0.3.3+11

Date: 2026-04-26

## Status

Build-truth and QA hardening release-test build.

## Changed

- Settings now shows both the UI build label and the Android APK metadata build label.
- Settings includes a `Build check` row so stale Dart snapshot / package metadata mismatches are visible inside the app.
- APK metadata is read on-device with `package_info_plus` and recorded in Diagnostics.
- The release workflow now treats emulator-visible build truth as mandatory before GitHub APK upload.
- Emulator smoke verified `UI build 0.3.3+11`, `APK build 0.3.3+11`, and `Build check Match`.

## Still needs device proof

- Confirm `UI build`, `APK build`, and `Build check` all show the expected values on the Poco X6 Pro.
- Continue comparing gallery and editor performance with Settings > Diagnostics.
