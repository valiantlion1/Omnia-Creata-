# Prototypes

This area is for concepts, scratch work, and incubating products that are not yet canonical core apps.

- `omniatools-lab/` incubates small tool ideas that may eventually converge into `OmniaTools`
- `subcompare-lab/` keeps the SubCompare prototype
- `website-temp/` and `web-scratch/` are explicitly non-canonical
